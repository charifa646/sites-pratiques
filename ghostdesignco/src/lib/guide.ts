/**
 * Where the ghost's head is on screen (CSS px), written by the 3D world every
 * frame. Kept free of three.js so DOM components can read it cheaply.
 */
export const guideState = { x: 0, y: 0, visible: false };
